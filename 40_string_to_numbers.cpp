#include<iostream>
using namespace std;



int main(){
    string strNumber = "1012";
    int number1 = stoi(strNumber);
    // int number2 = atoi(strNumber);

    // int index = size(strNumber) - 1;

    // while (index >= 0){
    //     char ch = strNumber.at(index);
    //     // int digit = (int) ch;
    //     int digit = ch.
    //     number = number * 10 + digit;
    //     index--;
    // }
    cout<<"Number formed is "<<number1<<endl;
    // cout<<"Number formed is "<<number2<<endl;
    return 0;
}